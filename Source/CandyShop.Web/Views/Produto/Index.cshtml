@model IEnumerable<CandyShop.Core.Services.Produto.Dto.ProdutoDto>

@{
    Layout = null;
}
<div>
    <form class="col s12" style="margin-left: -10%">
        <table class="table highlight">
            <tr>
                <th>
                    Nome
                </th>
                <th>
                    Preço
                </th>
                <th>
                    Qtde em estoque
                </th>
                <th></th>
            </tr>

            @for (var i = 0; i < 5; i++)
            {
                <tr>
                    <td>
                        @*@Html.DisplayFor(modelItem => item.NomeProduto)*@ Chocolate
                    </td>
                    <td>
                        @*@Html.DisplayFor(modelItem => item.PrecoProduto)*@ R$ 2,00
                    </td>
                    <td>
                        @*@Html.DisplayFor(modelItem => item.QtdeProduto)*@ 7
                    </td>
                    <td>
                        <a class="btn-floating btn-sm waves-effect waves-light blue darken-1"><i class="material-icons">info_outline</i></a>
                    </td>
                    <td>
                        <a class="btn-floating btn-sm waves-effect waves-light orange"><i class="material-icons">edit</i></a>
                    </td>
                    <td>
                        <a class="btn-floating btn-sm waves-effect waves-light red lighten-2"><i class="material-icons">delete</i></a>
                    </td>

                </tr>
            }
        </table>
    </form>
</div>

<style>
    tr {
        color: black;
    }
</style>